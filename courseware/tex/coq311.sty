%% Style file for Coq311.

\ProvidesPackage{3110}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Package Imports %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\RequirePackage{amsfonts}
\RequirePackage{amsmath}
\RequirePackage{amssymb}
\RequirePackage{amsthm}
\RequirePackage{algorithm}
\RequirePackage{algorithmicx}
\RequirePackage{algpseudocode}
\RequirePackage{enumerate}
\RequirePackage{fourier-orns}
\RequirePackage{graphicx}
\RequirePackage[colorlinks]{hyperref}
\RequirePackage{listings}
\RequirePackage{palatino}
\RequirePackage{xcolor}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%% For Typesetting Coq %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\definecolor{lightgray}{rgb}{.9,.9,.9}
\definecolor{darkgray}{rgb}{.4,.4,.4}

%% A minimal definition of the Coq language
\lstdefinelanguage{Coq}{
  %% Gallina Keywords
  keywords = {
    _,
    as,
    at,
    cofix,
    else,
    end,
    exists,
    exists2,
    fix,
    for,
    forall,
    fun,
    if,
    IF,
    in,
    let,
    match,
    mod,
    Prop,
    return,
    Set,
    then,
    Type,
    using,
    where,
    with
  }
  sensitive    = true,
  keywordstyle = \color{black}\bfseries,
  %% Keywords for the Vernacular
  ndkeywords   = {
    Axiom,
    Conjecture,
    Parameter,
    Parameters,
    Variable,
    Variables,
    Hypothesis,
    Hypotheses,
    Definition,
    Let,
    Inductive,
    CoInductive,
    Fixpoint,
    CoFixpoint,
    Theorem,
    Lemma,
    Remark,
    Fact,
    Corollary,
    Program,
    Proposition,
    Example,
    Proof,
    Qed,
    Admit,
    Admitted
  }
  ndkeywordstyle  = \color{black}{\bfseries},
  identifierstyle = \color{black},
  morecomment     = [s]{(*}{*)},
  commentstyle    = \color{lightgray}\ttfamily,
  stringstyle     = \color{darkgray}\ttfamily,
  morestring      = [b]',
  morestring      = [b]"
}

\lstset{
  language      = Coq,
  extendedchars = true,
  basicstyle    = \footnotesize\ttfamily,
  showstringspaces = false,
  showspaces = false,
  numbers = left,
  numberstyle = \footnotesize\ttfamily,
  numbersep = 8pt,
  tabsize = 2,
  breaklines = true,
  showtabs = false,
  captionpos = b,
  escapeinside = {\%*}{*)},
  frame = single,
}

\let\code\lstinline
\lstnewenvironment{coq}{}{}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Generic Macros  %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\coffeebreak}[1]{\vspace{0.1cm}\begin{center}\decoone~~~~#1~~~~\decoone\end{center}\vspace{0.1cm}}
